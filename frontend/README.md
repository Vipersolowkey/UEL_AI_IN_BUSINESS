# Hotel Management & Analytics Frontend

## Run locally

1. Install dependencies:
   `npm install`
2. Start the dev server:
   `npm run dev`
3. Make sure the FastAPI backend is running on `http://127.0.0.1:8000`
